import React from "react";

export default class index extends React.Component<any> {
  constructor(prpos: any) {
    super(prpos);
    console.log(this);
  }

  render = () => {
    return 1100;
  };
}
